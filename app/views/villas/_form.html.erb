<%= form_with(model: villa, local: true) do |form| %>
  <% if villa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(villa.errors.count, "error") %> prohibited this villa from being saved:</h2>

      <ul>
        <% villa.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
